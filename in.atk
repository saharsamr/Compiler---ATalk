actor salam<3>
    receiver init()



      foreach a in "pashm"
          if(a == 'h')
            break
          end
          write(a)
      end



                    foreach elem in {1,2,3}
                      foreach c in {4,5,6,7}
                          write(c)
                        end
                      end

        foreach a in "sadaf"
        write(a)
        end

    end
end
